.DS_Store
node_modules/
*.pem
*.key
lolly-root-key.pem
# Ignore every dotenv variant (.env, .env.local, .env.production, …) so a stray
# one can't be committed; keep the placeholder template tracked.
.env*
!.env.example
